export const metadata = {
  title: 'Кошик — CreaSphere',
  description: 'Кошик покупок в інтернет-магазині CreaSphere.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function CartLayout({ children }) {
  return children;
}
