export const metadata = {
  title: 'Замовлення оформлено — CreaSphere',
  robots: {
    index: false,
    follow: false,
  },
};

export default function OrderSuccessLayout({ children }) {
  return children;
}
