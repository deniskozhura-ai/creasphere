export const metadata = {
  title: 'Оформлення замовлення — CreaSphere',
  description: 'Оформлення замовлення в інтернет-магазині CreaSphere.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
