import AdminWorkshopsManager from '@/components/AdminWorkshopsManager';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Керування майстер-класами — CreaSphere Admin',
};

export default function AdminWorkshopsPage() {
  return <AdminWorkshopsManager />;
}
